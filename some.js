/* SOME:  se utiliza para evaluar si alguno de los elementos de un arreglo coinciden con la condicion que se quiere comprobar devolviendo un true o un false*/
const some = students.some((student) => student.name === 'Jack');
console.info(result);