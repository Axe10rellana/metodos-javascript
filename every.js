/* EVERY:  se utiliza para evaluar si todos los elementos de un arreglo coinciden con la condicion que se quiere comprobar devolviendo un true o un false*/
const every = students.every((student) => student.lastname === 'Doe');
console.info(every);